#include <stdio.h>

int main(int argc, char const *argv[]) {
    // int a[3];
    // int *iptr = &a[0];
    // int *ptr = a;

    // for (int i = 1; i < sizeof(a) / sizeof(a[0]); i++) {
    //     a[i] = i;
    // }
    // printf("%d, %d, %d\n", a[0], iptr[0], ptr[0]);
    // printf("%d, %d, %d\n", a[1], iptr[1], ptr[1]);
    // int i = 10;
    // int *iptr = &i;
    // int v = *iptr;
    // *iptr = 100;
}